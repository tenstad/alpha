fn fib(n) {
    if n <= 1 {
        n;
    } else {
        fib(n-1) + fib(n-2);
    }
}
print(fib(9));

let mut fib = 0;
let mut next = 1;
for i in [0..9) {
    let prev = fib;
    fib = next;
    next = prev + fib;
}
print(fib);