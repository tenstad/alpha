fn repeatpowtwo(f, n) {
    if n <= 0 {
        f;
    } else {
        repeatpowtwo(fn (a) { f(f(a)); }, n-1);
    }
}

fn powtwo(n) {
    let f = repeatpowtwo(fn (x) { x + 1; }, n);
    f(0);
}

print(powtwo(10));