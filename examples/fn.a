fn repeatpowtwo(f, n) {
    if n <= 0 {
        f;
    } else {
        repeatpowtwo(fn x(a) { f(f(a)); }, n-1);
    }
}

fn powtwo(n) {
    fn addone(x) { x + 1; }
    let f = repeatpowtwo(addone, n);
    f(0);
}

print(powtwo(10));
