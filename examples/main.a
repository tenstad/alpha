fn fib(n) {
    let result = if n <= 1 {
        n;
    } else {
        fib(n-1) + fib(n-2);
    };
    printf("fib(%d) = ", n);
    printf("%d\n", result);
    result;
}

printf("hello world\n", 0);
fib(8);
